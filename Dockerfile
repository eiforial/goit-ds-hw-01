# Используем базовый образ Python
FROM python:3.12

# Установка рабочей директории внутри контейнера
WORKDIR /app

# Копируем все файлы приложения в контейнер
COPY . .

# Опционально: Если ваше приложение является CLI-приложением, укажите команду для взаимодействия с ним
CMD ["python", "HW-1.py"]